export type Config = {
    numero_configuration: number,
    nb_joueurs: number,
    jeu: {
        numero_jeu: number,
        nom: string
    }
    extension: {
        numero_extension: number,
        nom: string
    }
}